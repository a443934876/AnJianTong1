<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include layout="@layout/item_title" />

    <LinearLayout xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical">

        <org.chinasafety.liu.anjiantong.view.widget.ArcProgress
            android:id="@+id/audio_progress_time"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:layout_marginBottom="16dp"
            app:arc_bottom_text="录音时间"
            app:arc_finished_color="#32cd32"
            app:arc_max="60"
            app:arc_progress="0"
            app:arc_suffix_text="秒"
            app:arc_unfinished_color="#3299cc" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <Button
                android:id="@+id/mediarecorder1_AudioStartBtn"
                android:layout_width="120dp"
                android:layout_height="wrap_content"
                android:layout_marginRight="8dp"
                android:layout_marginTop="10dp"
                android:background="@drawable/blue_button_background"
                android:padding="16dp"
                android:text="开始录制"
                android:textSize="18sp" />

            <Button
                android:id="@+id/mediarecorder1_AudioStopBtn"
                android:layout_width="120dp"
                android:layout_height="wrap_content"
                android:layout_marginLeft="8dp"
                android:layout_marginTop="10dp"
                android:background="@drawable/blue_button_background"
                android:padding="16dp"
                android:text="结束录制"
                android:textSize="18sp" />
        </LinearLayout>
    </LinearLayout>
</LinearLayout>